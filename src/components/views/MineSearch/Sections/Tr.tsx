import React from 'react';
import Td from './Td';

const Tr = (props:any) =>{
    const {rowData, rowIndex, dispatch} = props;
    
    return(
        <tr>
            <Td />
        </tr>
    );
}

export default Tr;